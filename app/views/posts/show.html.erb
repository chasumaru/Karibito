<h3>タイトル</h3>
<%= @post.title %><br>

<h3>本文</h3>
<%= @post.content %>

<% if @post.images.attached? %>
<div style="width: 700px;">
  <p><strong>Image:</strong></p>
  <% @post.images.each do |image| %>
    <%= link_to image_tag(image, size: '200×150'), url_for(image) %>
    <%= image_tag image.variant(resize: "300×300") %>
  <% end %>
</div>
<% end %>

<%= link_to 'Edit', edit_post_path(@post) %> |
<%= link_to 'Back', posts_path %>

投稿したユーザーのみ投稿を編集・削除が可能
