<!DOCTYPE html>
<html>
    <head>
        <title>Karibito</title>
        <%= favicon_link_tag('favicon.ico') %>
        <meta name="viewport" content="width=device-width,initial-scale=1">
        <%= csrf_meta_tags %>
        <%= csp_meta_tag %>
        <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
        <%= javascript_importmap_tags %>
        <%= yield(:head) %>
    </head>
<%# ブレイクポイント対応はしていない %>
    <body class="font-body text-stone-700 bg-lime-300 relative z-0">
        <header data-controller="sidebar" id="header">
            <%= render "shared/navbar" %>
        </header>
        <main>
            <div class="bg-neutral-100 mx-auto relative z-0 pt-24">
                <%= render "shared/flash" %>
                <%= yield %>
            </div>
        </main>
        <footer>
            <%= render "shared/footer" %>
        </footer>
    </body>
</html>