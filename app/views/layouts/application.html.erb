<!DOCTYPE html>
<html>
  <head>
    <title>Karibito</title>
    <%= favicon_link_tag('favicon.ico') %>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <%= yield(:head) %>
  </head>
<%# ブレイクポイント対応はしていない %>
  <body class="font-body text-stone-700 bg-neutral-100 relative z-0 overflow-x-hidden">

        <header class="w-full bg-lime-300 top-0 fixed h-14 z-40" data-turbo="false">
            <div id="hamburger" class="hamburger">
                <span class="inner_line top-0" id="line1"></span>
                <span class="inner_line top-2.5" id="line2"></span>
                <span class="inner_line bottom-0" id="line3"></span>
            </div>
            <nav id="nav" class="z-30 absolute h-screen w-40 -right-40 top-0 bg-white transition duration-600">
                <div class="nav_contents pt-20">
                    <p class="block px-4 py-2 bg-neutral-400 text-white">アカウント設定</p>
                    <ul class="space-y-2 border-gray-200 outline-none">
                        <% if user_signed_in? %>
                        <li class="px-4 py-2 rounded hover:bg-gray-100 dark:hover:bg-gray-700 no-tap-highlighting"><%= link_to 'マイページ', pages_show_path, class: "block"%>
                        <li class="px-4 py-2 rounded hover:bg-gray-100 dark:hover:bg-gray-700 no-tap-highlighting"><%= link_to 'プロフィール編集', edit_user_registration_path, class: "block" %> 
                        <li class="px-4 py-2 rounded hover:bg-gray-100 dark:hover:bg-gray-700 no-tap-highlighting"><%= link_to 'アカウント設定', edit_user_registration_path, class: "block" %> 
                        <li class="border-b  px-4 py-2 rounded hover:bg-pink-100 dark:hover:bg-gray-700 no-tap-highlighting"><%= button_to "ログアウト", destroy_user_session_path, { method: :delete, form: { data: { turbo_confirm: "ログアウトしますか？" } } } %> 
                        <% else %> 
                        <li class="px-4 py-2 rounded hover:bg-gray-100 dark:hover:bg-gray-700 no-tap-highlighting"><%= link_to '会員登録', new_user_registration_path, class: "block" %> 
                        <li class="px-4 py-2 rounded hover:bg-gray-100 dark:hover:bg-gray-700 no-tap-highlighting"><%= link_to 'ログイン', new_user_session_path, class: "block" %> 
                        <% end %>
                    </ul>
                    <p class="block px-4 py-2 -mb-4 bg-neutral-400 text-white font-semibold">サービスについて</p>
                    <ul class="mt-4 space-y-2 border-gray-200">
                        <li class="px-4 py-2 rounded hover:bg-gray-100 dark:hover:bg-gray-700 no-tap-highlighting"><%= link_to 'Karibitoとは?',pages_show_path, class: "block" %> 
                        <li class="px-4 py-2 rounded hover:bg-gray-100 dark:hover:bg-gray-700 no-tap-highlighting"><%= link_to '利用規約',pages_show_path, class: "block" %> 
                        <li class="px-4 py-2 rounded hover:bg-gray-100 dark:hover:bg-gray-700 no-tap-highlighting"><%= link_to 'お問い合わせ',pages_show_path, class: "block" %>
                        <li class="px-4 py-2 rounded hover:bg-gray-100 dark:hover:bg-gray-700 no-tap-highlighting"><%= link_to 'よくある質問',pages_show_path, class: "block" %>
                    </ul>
                </div>
            </nav>
            <h2><%= link_to "Karibito", root_path, class:"block absolute top-3 left-18 font-semibold no-tap-highlighting"%></h2>
            <%= link_to image_tag('Karibito_logo.png', class:'md:w-18 w-12 absolute top-1 left-3 no-tap-highlighting'), root_path %>
        </header>
        <div class="container mx-auto relative z-0 mt-20">            
            <% if flash[:notice] %>
                <p class="px-6 py-4 bg-blue-50 rounded-lg text-blue-500"><%= notice %></p>
            <% end %>
            <% if flash[:alert] %>
                <p class="px-6 py-4 bg-blue-50 rounded-lg text-blue-500"><%= alert %></p>
            <% end %>
            <%= yield %>
        </div>
        <footer>
            <div>
                <p>fottter要素</p>
            </div>
        </footer>
    </body>
</html>
